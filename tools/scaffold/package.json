{
  "name": "@repo/scaffold",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "scaffold": "pnpm --filter @repo/schemas build && tsx src/scaffold.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@repo/schemas": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@repo/config": "workspace:*",
    "@types/node": "^22.10.5",
    "eslint": "^9.18.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
