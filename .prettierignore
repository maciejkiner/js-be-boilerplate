# Generowane / zewnętrzne — nie formatujemy
pnpm-lock.yaml
**/dist/**
**/.turbo/**
coverage/
playwright-report/

# Specyfikacja to artefakt wejściowy (źródło prawdy) — nie ruszamy jej formatowania
spec/

# Klient generowany z OpenAPI — źródłem jest schemat; format musi zgadzać się z generatorem
# (CI regeneruje i robi `git diff`), więc Prettier tych plików nie dotyka.
apps/api/openapi.json
packages/api-client/src/generated/**

# Metadane migracji pisane przez drizzle-kit — sformatowanie ich rozjechałoby się przy każdym
# `db:generate` (i wywracało `format:check`).
apps/api/drizzle/meta/**
