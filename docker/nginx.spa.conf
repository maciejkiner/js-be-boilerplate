server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA (TanStack Router, routing kliencki): deep-linki (np. /projects/<id>) muszą
  # trafić do index.html, a nie dawać 404.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
