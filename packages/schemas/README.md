# packages/schemas

Placeholder. Schematy Zod encji i formularzy + metadane. Czysty TS, zero zależności.
Jedno źródło prawdy dla kształtu danych (baza, walidacja BE/FE, typy, OpenAPI, kolumny, formularze).

Wypełniane w **Fazie 4** (encja referencyjna). Patrz `PLAN.md`.
