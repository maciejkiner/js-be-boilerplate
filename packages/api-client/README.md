# packages/api-client

Placeholder. Klient TS generowany z OpenAPI; framework-agnostic (`fetch`).
Bez routera i `import.meta.env` — baseURL/env wstrzykiwany jawnie.

Wypełniane w **Fazie 5**. Patrz `PLAN.md`.
